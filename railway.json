{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && python manage.py import_lib_csv && manage.py shell -c \"from users.models import User; User.objects.create_superuser('admin', 'radchenko.dim@gmail.com', 'admin')\" && python manage.py collectstatic --noinput && gunicorn --timeout 500 maj_lib.wsgi"
    }
}