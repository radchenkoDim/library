{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && manage.py shell -c \"from django.contrib.auth.models import User; User.objects.create_superuser('admin', 'radchenko.dim@gmail.com', 'admin')\" && python manage.py collectstatic --noinput && gunicorn --timeout 500 maj_lib.wsgi"    
    }
}